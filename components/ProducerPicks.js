'use client'

// TODO: Enable when producer_picks table exists on the master Australian Atlas portal.
// Producer picks on SBA are already handled natively on the venue page via the
// producer_picks / producer_pick_venues tables in the SBA Supabase DB.

export default function ProducerPicks({ venueId, venueName, vertical }) {
  return null
}
